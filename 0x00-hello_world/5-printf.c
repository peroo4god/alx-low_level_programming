#include <stdio.h>
/**
 * main - this is the main function read by the compiler.
 *
 * Return: return 0 if the program run successfully
 */

int main(void);

{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
