#include "main.h"

/**
 * print_sign - used to prints the sign of a number
 * @n: the int to check
 * Return: 1 and print + n is greater than zero
 * 0 and print 0 if n is zero
 * -1 and print - if n is less than zero
 */
in print_sign(int n)
{
	if (n > 0)
	{
		_putchar("+");
		return (1);
	} else if (n == 0)
}
